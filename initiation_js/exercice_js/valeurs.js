var a = 2; //Déclarer une variable
a--; // Décrémenter la variable
console.log(a); // Afficher le résultat dans la console
a++; // Incrémenter la variable
console.log(a); // Afficher le résultat dans la console

var b = 8; //Déclarer une variable
// b = b + 2;
b += 2; //Ajouter 2 unités à cette variable
console.log(b);

var c = a + b; //Déclarer un variable "c" et affecter la somme des deux variable précédantes
console.log(c);

c = c * b; //Surcharger la variable "c" en lui affectant la multipilcation de la variable elle même avec la variable "b"
console.log(c);
